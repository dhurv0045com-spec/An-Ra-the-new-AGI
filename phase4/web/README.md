# AN-RA Phase 4 Web

Minimal operator interface for the AN-RA API.

This file is intentionally short on the `iterate500` branch. The branch keeps the
web component registered so system structure checks can prove the API/UI surface
is still connected while training focuses on the 500M frontier experiment.
