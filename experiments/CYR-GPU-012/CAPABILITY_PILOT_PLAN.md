# CAPABILITY-PILOT-001 — bounded arithmetic curriculum pilot

PREEXECUTION. Exploratory, single checkpoint, not AGI or a cognitive-mechanism test.
Supersedes the unexecuted MECHANISM_PROBE_PLAN.md; that probe has not run.

Parent: CYR-GPU-012 FINAL model SHA256
94e82920eaf03089630b85b9061bd0fa680b629c0bcd526e58bf301712127a99.
Frozen model/backend code at C:/Users/ankit/cyr012-frozen, commit
1a1624ed4c62e1a176dc4bed97e628f0b648a31b. Parent checkpoint remains immutable.

Intervention: 600 further updates, batch64, 32 original training rows plus
32 supplemental rows per update, independent CPU generator seed 8121.
Supplement: enumerate a<b in 10..79, sum<=99; include carry and non-carry,
same-band and cross-band cases, both operand orders. Exclude unordered pairs
from ALL original train/controller/measurement/reserved partitions and all
52 prior matched-band diagnostic pairs. No altered prompt format.

This exposes formerly unseen bands 6/7 in training. Original test pairs remain
untrained, but these tests become pair-held-out interpolation tests, NOT the
original unseen-band extrapolation task. Scores cannot replace CYR-012's verdict.
Answer labels for training are generated by arithmetic, never copied from heldout
predictions. This is supervised curriculum, not autonomous self-improvement.

Initialize from parent weights; fresh canonical AdamW lr=.001, betas .9/.95,
eps1e-8, canonical weight-decay groups, clip1. CUDA BF16 training/FP32 parameters;
greedy FP32 inference via frozen evaluator. No optimizer-state resume claim.
Before/after only: original controller64, STANDARD85, original train probe100,
matched-band104 (52 pairs). No reserved evaluation. Before outputs must match
saved controller/STANDARD/matched-band receipts by unique world ID and stop.
Log all final raw predictions, and save/reload final model to verify predictions.

Fixed success rule: both controller and STANDARD improve by >=.10 absolute,
and train-probe/matched-band exact each decline by <=.05. Otherwise report
NO_PRESPECIFIED_GAIN (with literal deltas). Not G90, not cognition, no promotion.
Single arm cannot attribute gain to curriculum separately from extra updates or
fresh optimizer. No tuning, retrying, selecting an intermediate checkpoint or
extending training on the basis of scores. A matched control and fresh heldout
tasks are required for stronger claims. V11 reproduction remains unresolved.

Budget: 600 updates only; total wall cap 10min including evaluation; 2 CPU threads,
CUDA allocation cap3GiB; abort if free RAM<2GiB or sampled GPU temperature>=85C.
Preflight requires free RAM>=3GiB, free GPU>=4GiB. Guard every 25 updates and
before evaluation chunks. Incomplete/invalid execution is not a negative result.
Hash source, plan, parent, manifest and generated dataset BEFORE training.
Output outside git: C:/Users/ankit/cyr012-evidence/capability-pilot01.
No commit/push, no changes to production model or original evidence.
