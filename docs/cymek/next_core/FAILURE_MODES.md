# FAILURE MODES (red-team of the winner, V5.1)

Assume the chosen architecture will fail. For each mode: SIGNATURE / DETECTION / LIKELY CAUSE / CHEAPEST DIAGNOSTIC / ROLLBACK.

| Mode | Signature | Detection | Likely cause | Cheapest diagnostic | Rollback |
|---|---|---|---|---|---|
| representation failure | train M99 + 0% held-out (the CYR-011 production signature) | scheduled candidate-free held-out eval every 150 updates | class-space/output-space regime; NOT block design | R1C-class MASK diagnostic on a 4L/128w slice | none needed — this is the current known state; the experiment tree owns it |
| output-space failure (if treatment adopted later) | structural learning with inactive rows winning at inference | ACTIVE_ONLY vs full-vocab dual endpoints (R1C design) | train/inference likelihood mismatch | ACTIVE_ONLY diagnostic at every scheduled eval | revert to canonical full-softmax path (one contract flag, hash changes) |
| optimizer instability | loss spikes; grad-norm outliers; clip saturation rate rising | per-update certify_real_update telemetry | LR regime mismatch; schedule bug | LR-vs-loss curve at bounded warmup; certify receipts | step back to last good milestone checkpoint; LR floor |
| gradient scaling / reduction-order drift | post-clip norm ≠ 1.0 beyond 1e-4; resume hash mismatch | clip certificate every update; exact-resume smoke | fp32 reduction order; kernel nondeterminism | CPU oracle vs device reduction (xla_accumulation_oracle pattern) | deterministic-kernel path + tolerance derivation (R1C precedent) |
| attention instability | attention entropy collapse; NaN under long context | entropy telemetry; finite checks every eval | RoPE/context interaction at 4,096 | 4k-context forward canary (existing) | reduce context; investigate; blocks unaffected |
| tokenizer mismatch | zero-UNK invariant broken; packing efficiency drop | tokenizer identity hash; pack receipt | artifact/spec divergence | encode/decode roundtrip + hash check | re-pin artifact; abort run (fail-closed) |
| data mismatch | family/token ledger drift; contamination hits | manifest audit; 8-gram scan; family ledger | unqualified corpus | CITADEL-DATA-001 audit suite | refuse the corpus (fail-closed); regenerate |
| memorization without generalization | train exact 1.0, held-out 0 at saturation + beyond | milestone held-out evals past the delayed region (AD24) | underdosed exposure OR representation regime (see row 1) | continue past ~9k-18k-update region with probes | not a rollback: a measurement against AD24 |
| retention collapse | robust-min fall on old-skill CONTROL probes | 25-50-update observation cadence (V4 cadence) | narrowed high-plasticity support | paired robustness probes (ARK-015 axes) | escalate sparse treatment-exact replay (external); never lower LR into collapse (ARK-010) |
| evaluation shortcut | a heuristic baseline scores 1.000 (the CITADEL-DATA-001 signature) | attack battery on every new surface before use | generator answer-placement rule | tools/citadel_eval_attack.py + audit_tiered_corpus.py | reject the surface; regenerate |
| EOS failure | generations ending MAX_TOKENS (the T1D signature, 15,000/15,000) | complete-answer rate reported separately from content | missing EOS supervision | stop-reason histogram | contract is LOCKED; failure indicates a contract violation in the caller, not the Core |
| checkpoint corruption | hash/schema validation failure at load | store validation on every open | interrupted write; stale writer | store tamper probe | single-writer fence + never-overwrite rotation (V5 store) |
| resume divergence | resumed trajectory ≠ uninterrupted hashes | fresh-process next-update agreement smoke | missing RNG/counter state | state-identity diff (12 hashed classes, ARK-020 V3 pattern) | restore from parent-fenced checkpoint |
| compute overrun | wall budget exceeded mid-campaign | progressive fixed-wall planner (CYR-009 lesson) | optimistic calibration | pre-outcome T4 calibration ×1.35 factor | checkpoint + partial bundle + resume (fail-closed, science unchanged) |
| memory OOM | CUDA OOM at frozen batch | batch×ctx memory model (peak ≈ 18 bytes/param at adopted layout) | activation spike at 4k ctx | activation-checkpointing flag (already in core) | microbatching; activation checkpointing; DOES_NOT_FOLD classification |
| scaling failure | formation doesn't improve with scale while smaller probes do | scale-ladder canaries (SCALING_PLAN) | representation bound, not capacity (B32 alternative) | CS-TRANSFER-001 scale arm | halt scale plan; representation line continues |
