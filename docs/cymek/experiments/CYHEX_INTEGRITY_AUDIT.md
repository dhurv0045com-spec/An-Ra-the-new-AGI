# Cyhex integrity audit — evidence archive milestone

Baseline: `e60e3dbc420f35c4abdc9c3de385c4d3e6692a19`, local
`origin/cyhex-hermes`. Work isolated on `codex/cyhex-integrity-audit`.
This is a bounded engineering milestone, NOT completion of the whole research
roadmap, an exact-HEAD full-suite receipt, or a new experiment outcome.

## Verified

- Shared checkout was dirty: modified `agent.md`, untracked CURRICULUM-001
  runner/helper/tests/result receipt. None was copied, staged or modified here.
- CYR-011 and CYR-012 original local ZIPs both pass `verify_bundle` against
  the tracked receipts, including SHA-256, byte count, exact member list and
  JSON parsing. This establishes archive identity, not scientific causality.
- Reproduced verifier defect: repeated ZIP member names caused name-based
  reads to validate the final entry twice. The first entry's contents were
  not checked. New regression failed with `DID NOT RAISE`; 3 controls passed.
- Two-line repair rejects repeated archive member names before reading JSON.
  Unique valid archives remain accepted; malformed JSON and duplicate
  declarations remain rejected. Neither historical ZIP has this defect.
- Existing TIE-ROLE decision and bounded CPU checkpoint/package integration
  tests passed. This does not certify all pilot launch prerequisites.

## Contradicted / stale documentation

- `agent.md:3` names `cymek-500m-readiness`, not this baseline branch.
- `EXPERIMENT_LOG.md:59` says CYR-005 pending; `agent.md:105` says superseded.
- `FAILURES.md:17` says CYR-001 pending; `EXPERIMENT_LOG.md:40` says superseded.
- FORMATION-MUX `RETURNED_BUNDLE_AUDIT.md:40-43` uses "exonerated" despite
  its own zero-baseline interpretation at lines 14-24. A floor-limited contrast
  cannot exonerate a mechanism. The official INCONCLUSIVE interpretation and
  frozen endpoints must stand; diagnostics cannot retroactively replace them.
Historical documents have not been silently rewritten.

## Implemented but not fully verified / blocked / owner-required

- CYR-011/012 discrepancy: bundle identity and the difference ledger are now
  established (see below). Initial-tensor equivalence and kernel selection
  remain UNKNOWN; no causal ranking exists; no retraining was performed.
- FORMATION-DIAG implementation and tests exist. Preserved checkpoint custody
  and complete diagnostic coverage have not been established by this milestone;
  no checkpoint recovery request or metric outcome is claimed yet.
- HORM source/artifact audit remains separate and incomplete in this record.
- Pilot and full frontier require explicit authorization; neither was launched.
  Production, PRE500M, paid hardware, and large training gates remain unchanged.

## Reproducibility ledger (Phase 1 partial closure)

Machine-checkable comparison `docs/cymek/experiments/CYR-GPU-012/
CYR011_VS_CYR012_LEDGER.json` (schema `anra-cyr-repro-ledger/v1`), generated by
`anra_v5/cyr_repro_ledger.py` from the two SHA-verified original bundles after
fail-closed load (receipt experiment match, bundle bytes/SHA/member list, JSON
parse). 15 fields classified with explicit epistemic limits; unknowns preserved:

- PROVEN IDENTICAL: torch 2.11.0+cu128; model_seed 3301; order_seed 4701;
  batch_rows 64; semantic_stream_sha256 `12e471f4…560ea` (digest identity does
  NOT independently prove row contents); g90_confirm_update null (neither
  reached G90); initial_l2 46.88997268676758 (scalar norm match does NOT prove
  tensor identity).
- PROVEN DIFFERENT: frozen_executable_sha (receipt identity only); gpu_name
  (Tesla T4 vs RTX 4050 Laptop); updates (8,081 vs 18,000); row_presentations
  (517,184 vs 1,152,000); g50_confirm_update (2,200 vs null — an OUTCOME
  difference, not itself causal evidence); final_model_sha256.
- UNKNOWN (not recoverable from bundles): initial_tensor_sha256 (requires
  initial-tensor replay); kernel_selection (no kernel trace recorded).
  Initial-tensor replay has since been EXECUTED — see
  "Initial-tensor replay (Phase 2 executed)" below.

Newly verified from the frozen worktree Git: the executable diff
`0a97257e..1a1624e` touches exactly 6 files (+406/−6): `anra_v5/cyr_gpu011_run.py`
(stop_on_g90 parameter + current-streak G90 qualification), new CYR-012 closure
battery/scoring module and PLAN, and two test files (stop_on_g90 parametrization).
No v5_model, v5_data, optimizer, or objective file changed. This confirms the
RESULT.md claim that the diff is limited to probes/stopping/operator/tests —
model and optimizer math are byte-identical between the two frozen executables.

Bounded first-divergence observation (trace, update 200, both bundles):
dev_controller 9.375% (V11) vs 4.6875% (012); measurement 11.76% vs 1.18%;
train probe 65% vs 82%; relative displacement 0.2367 vs 0.2280 — near-identical
global weight movement with already-divergent scores. This bounds the first
OBSERVED divergence at the first scheduled evaluation and shows the trajectories
were not globally different at that point; it does not identify a cause.

## Validation (run from isolated worktree)

RED command (duplicate-member regression):
`env -u PYTHONPATH -u PYTHONHOME py -3.14 -m pytest tests/test_v5_cyr_gpu012_duplicate_members.py -q`

Before repair: **1 failed, 3 passed in 0.41s** (expected duplicate-member rejection missing).

GREEN/integration command:
`env -u PYTHONPATH -u PYTHONHOME OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 py -3.14 -m pytest tests/test_v5_cyr_gpu012_duplicate_members.py tests/test_v5_cyr_gpu012_evidence.py tests/test_v5_cyr_gpu012_closure.py tests/test_tie_role_pilot_001_decision.py tests/test_tie_role_pilot_001_integration.py -q`

**23 passed, 1 warning in 20.93s**. Torch emitted a CUDA build/compute-capability
warning during the CPU integration test. Not a GPU qualification result.
`git diff --check`: passed. No full-suite or remote-CI claim.

RED command (ledger, before module existed):
`ModuleNotFoundError: No module named 'anra_v5.cyr_repro_ledger'` — collection error.

Ledger TDD sequence (each step executed and observed):
1. 5 fixture tests written → red (module missing) → module → **5 passed**.
2. CLI test written → red (`no attribute 'main'`) → CLI added → **6 passed**.
3. Real-bundle run exposed dict-vs-string compare → regression test written →
   red (`proven_different` == `proven_identical`) → path fixed → green.
4. Uniqueness assertion strengthened (list-vs-set length) → red (16 rows, 15
   unique fields — legacy duplicate append) → duplicate removed → **6 passed**.
5. Final combined run: **16 passed in 0.56s** across ledger + archive/evidence
   suites; real-bundle CLI run then classified 15 fields from the two
   SHA-verified ZIPs. Recorded defects were reproduced before each fix; the
   historical duplicate-member ZIP defect does not affect either real bundle.

## Initial-tensor replay (Phase 2 executed)

`docs/cymek/experiments/CYR-GPU-012/CYR011_VS_CYR012_INITIAL_REPLAY.json`
(schema `anra-cyr-initial-replay/v1`), generated by `anra_v5/initial_replay.py`
from the two SHA-verified bundles after fail-closed load. Method: rebuild the
compact initialization from the frozen sources (`cyr012-frozen`, byte-identical
`v5_model/core.py`, `v5_model/initialize.py`, `v5_experiments/cyr_gpu011.py`)
under CPU `fork_rng`/seed 3301, twice, inside one process, then classify the
computed initial L2 against the recorded anchor.

Measured: element_count 987392 (= compact parameter receipt);
computed_initial_l2 46.88997268676758 == recorded 46.88997268676758
(**norm_delta 0.0**); repeat tensor digest identical
(`e263801d1b48d4f7a1d33d1e1be24f8296c6fa023268546a868ae25c9504df04`);
seed 3302 produces a different digest (test-verified); torch 2.11.0+cu128
matches both bundles. Verdict **INITIALIZATION_REPLAYS_MATCHING_NORM**.

Limits (recorded in the artifact): neither bundle contains an initial-tensor
hash, so historical tensor identity is not directly provable; a matching
replay norm with byte-stable in-process reproduction SUPPORTS identical
historical initialization but does not prove it. A controlled cross-process
comparison on the two historical GPU environments (T4 vs RTX 4050) remains
the only direct test of the environment hypothesis and requires owner
authorization; no causal ranking is claimed.

## Consequence and next action

Ambiguous evidence archives now fail closed without changing experiment
semantics. The 011-vs-012 discrepancy is now a bounded, machine-checkable
ledger: model/optimizer/data math are byte-identical across the two frozen
executables; seeds and sampling digest match; the initialization replay
matches the recorded anchor exactly (norm_delta 0.0, byte-stable repeat).
The remaining UNKNOWN is kernel selection on the two historical GPUs —
locally untestable; a controlled cross-GPU comparison would require owner
authorization. No causal ranking is claimed; no training was run;
pilot/frontier/production gates unchanged.

Operational disclosure: one read-only `git ls-remote` was mistakenly issued
before isolation despite the no-external-services boundary. It changed no
remote state; subsequent work used local refs only.
