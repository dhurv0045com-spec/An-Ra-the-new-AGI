# CYR-GPU-008 preoperator closure

CYR-GPU-008 is the successor to two non-scientific preexecution failures. CYR-GPU-006 reached real Colab calibration and then correctly failed its hardware-feasibility gate before any optimization because its frozen worst-case scope did not fit the 170-minute wall. CYR-GPU-007 was never run; it was superseded during final source audit after a compatibility decision wrapper was found capable of recursively re-entering itself.

## Frozen identities

- Executable Commit A: `504fe13e735e71baa3a01898f42930486067e5be`.
- Preregistration Commit B: `703a94114f34a6f9115d59f7b847722642378e61`.
- Canonical Cymek authority: `28bf57a0d299a2c13a99fe0046616c00a1b8530c`.
- Arkenstone audit authority: `6acd9dcbdd28d00f387ffcd004253a813aca4b66`.
- Discovery V6 bundle SHA256: `1ec3224075de49b080e229111e4c4f897430c3eca888c487116aff07a65c8d15`.

## Scientific contract retained

The hardware adaptation does not change the causal core. Every retained tier uses the real Cymek V5 implementation and the frozen 24,576 tokenizer. Acquisition is HIGH-LR and candidate-free G90 requires three sustained confirmations. A qualified acquired state is forked into HIGH, LOW, fixed-time HIGH->LOW and hysteretic HIGH/LOW continuations from identical model/optimizer bytes and an identical future stream. Exposure and fixed-time switching are measured in actual real tokens. A scientific comparison needs at least two independent parent subjects.

If hardware affords the transfer stage, the transfer comparison is prospectively fixed to equal-age/equal-exposure `HYSTERETIC_HIGH_LOW` versus `LOW_CONTINUE` from the same source parent, then both move to HIGH LR on the same order-augmented registry-binding + old-T2 replay stream. If transfer cannot fit, the resolver may choose the preregistered two-parent retention-only tier; that tier cannot make a multi-task research-candidate claim.

## Hardware adaptation

Resolution uses only calibration status, measured training real-tokens/sec, measured candidate-free generation examples/sec, and the fixed wall budget. It never reads scientific loss, accuracy, G90 timing or treatment outcomes.

The resolver searches non-TINY proxies first across three preregistered scopes: `FULL_3P_TRANSFER3`, `CORE_2P_TRANSFER2`, and `RETENTION_2P_ONLY`. `TINY` is a last-resort development proxy and is mechanically prevented from becoming a production-facing research candidate. All scopes retain the 2M actual-token acquisition floor and 500k actual-token continuation floor; transfer, when enabled, retains a 500k actual-token/state floor.

## Validation evidence before final receipt-only closure

Dedicated CYR-GPU-008 CI run `34340001642` passed: all 3 notebook code cells compile and all 22 deterministic 008 + inherited scientific contract tests pass.

Full Cymek production-contract run `34340001662` executed the current preregistered tree and produced `533 passed, 1 skipped, 40 subtests passed`. Its only failing test was `test_exact_head_test_receipt`, which correctly rejected the historical closure receipt because it described the older CYR-GPU-006 tree. No model, optimizer, tokenizer, XLA-oracle, CYR-008 or notebook scientific/software test failed.

The final closure sequence intentionally leaves that verifier strict: this operator/readiness metadata is finalized first, the exact tree is revalidated, and then the closure receipt is refreshed in a receipt-only final commit. No executable or preregistered science is changed during closure.

## Claim boundary

No CYR-GPU-008 scientific GPU run has happened yet. There is no CYR-GPU-008 TPU evidence. PRE500M and the 500M campaign remain unauthorized. Production data remains `DATA_NOT_READY`. A positive GPU result is development evidence and must be audited before any TPU or production recipe decision.
