# CYR-GPU-008 THREATS

- Hardware tier/proxy/dose resolution may use only pre-science calibration fit/throughput and the fixed wall budget.
- At least two independent parents are required for any replicated retention claim.
- Retention-only and TINY tiers have a mechanical claim ceiling and cannot become production-facing research candidates.
- All four retention policies still fork from identical model+optimizer state and consume the same continuation stream.
- LOW may be near-freezing rather than consolidation; inherited displacement/optimizer-state diagnostics remain part of interpretation.
- Hysteresis cadence is fixed by the chosen hardware tier before outcomes.
- Transfer, when enabled, uses equal-age/equal-exposure HYST and LOW states from the same parent and requires at least two complete parent pairs.
- No-replay cross-task protection is not claimed; fixed replay is retained because ARK-013 already exposed the no-replay failure mode.
- Timeboxed/incomplete arms cannot enter a replicated decision.
- Calibration JSON is persisted before resolution, so another feasibility rejection remains useful evidence.
- The 007 recursion defect is explicitly regression-tested by executing the compatibility patch and invoking final decision inside it.
- A GPU result never certifies XLA/TPU semantics and cannot authorize PRE500M or 500M.
