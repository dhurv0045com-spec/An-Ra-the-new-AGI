# CYR-GPU-008 PLAN

CYR-GPU-008 is the corrected hardware-adaptive successor to CYR-GPU-007. 007 was never executed; a final preexecution review found a possible recursive final-decision path in its compatibility monkeypatch. 008 fixes that before any operator run and adds a regression that exercises the actual compatibility context.

The scientific question and causal invariants remain unchanged from 007: one HIGH acquisition per seed, candidate-free sustained G90, four same-parent/same-optimizer/same-future-stream retention forks, actual-token exposure, replicated matched-parent decisions, fixed-time actual-token control, hysteresis, post-decision sealed measurement, and equal-age HYST-vs-LOW order-robust binding transfer when hardware permits.

Hardware resolution is prospectively limited to calibration status, measured training real-tokens/sec, measured candidate-free generation examples/sec, and the 170-minute wall. It first searches MIDI/MICRO/RESEARCH_SMALL across the full 3-parent transfer tier, 2-parent transfer tier, and 2-parent retention-only tier. Only if no non-TINY tier fits may TINY be selected. TINY and retention-only resolutions can never set `research_candidate=true`.

Dose floors remain 2M actual acquisition tokens per parent, 500k continuation tokens per fork, and 500k transfer tokens per state when transfer is enabled. Hardware may increase dose prospectively toward 4M/2M/1M but may not reduce below floors.

Any GPU result remains development evidence only. PRE500M, 500M, TPU claims, production scheduler promotion, and future 5B-corpus work remain unauthorized.
