# CYR-GPU-009 threats

1. **TINY scale.** On a slow free GPU the resolver may select TINY. Any result is development evidence only; `research_candidate_possible=false`.
2. **No G90 event.** A parent that does not reach candidate-free sustained G90 is not a retention subject. This is returned as valid negative/insufficient-event evidence, not a runtime failure.
3. **Timebox.** A continuation arm that does not reach its target is excluded from the matched verdict. Partial receipts remain in the bundle.
4. **Arm-order bias.** HIGH runs first for seed 707 and LOW first for seed 808.
5. **Near-freezing.** LOW may protect simply by moving parameters less. Existing displacement, gradient, and Adam-moment telemetry is retained; 009 does not claim a consolidation mechanism.
6. **Task specificity.** This is T2 arithmetic retention only. ARK-013/014 already warn that cross-task plasticity and non-arithmetic transfer are separate questions.
7. **Hardware adaptation after calibration.** Only hardware throughput affects proxy selection. Scientific losses/accuracies never enter resolution.
8. **One-shot priority.** 009 deliberately omits FIXED/HYST/transfer to finish the highest-value matched pair twice instead of creating another preexecution feasibility failure.
