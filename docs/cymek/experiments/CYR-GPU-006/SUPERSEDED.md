# CYR-GPU-006 — SUPERSEDED AFTER PREEXECUTION CALIBRATION

Status: `SUPERSEDED_AFTER_PREEXECUTION_CALIBRATION_BEFORE_SCIENTIFIC_EXECUTION`.

No acquisition, retention, transfer, sealed evaluation, GPU scientific result, TPU run, PRE500M run, or 500M training occurred under CYR-GPU-006.

The operator ran Cell 0 on the assigned Colab GPU. The frozen 006 hardware-only resolver rejected every calibrated scientific proxy because the preregistered worst-case campaign — 3 acquisition parents, four matched retention forks per parent, and 3 equal-age binding-transfer parent pairs — could not fit the 170-minute hard wall at the measured throughput. The exact fail-closed exception was: `no calibrated proxy affords 3 acquisition parents, four matched retention forks, and the equal-age robust-binding plasticity comparison inside 170 minutes`.

This is not a negative scientific result. It is hardware feasibility evidence only. Because adapting the executable/resolver after Commit B would violate the 006 freeze, 006 is preserved unchanged and CYR-GPU-007 is a new preregistered identity.

CYR-GPU-007 keeps the causal invariants but preregisters hardware-only tiers: 3-parent full campaign when affordable; 2-parent full causal campaign when necessary; or a 2-parent replicated same-task retention campaign when transfer cannot fit. TINY is calibrated only as a last-resort development proxy and can never produce a production-facing research candidate.
