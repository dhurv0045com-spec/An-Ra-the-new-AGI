# MASTER GPU V5 — VALIDATED RESULTS

Imported 2026-09-08 from `ARKENSTONE_V5_RESULTS.zip` (SHA256 `e40355e0e0212406985c2445d2a8388b5d704ad722fd4c4b4c3455a5e64f0c7f`). All 9 uploaded JSON receipt hashes independently validated.

## ARK-007R — fresh retention replication

Three fresh acquisitions (909/1010/1111), four matched continuation orders each. HIGH `1e-3` produced collapse90 in **9/12** pairs; LOW `1e-5` produced **0/12**. Risk difference LOW−HIGH = **−0.75**; 9 discordant HIGH-fail/LOW-stable pairs, zero reverse. Protective direction held on every acquisition checkpoint.

**Status:** `REPLICATED_MICRO_TASK_RETENTION_EFFECT`. Strong causal evidence on Micro T2 only. Low LR moves parameters far less, so near-freezing remains a major mechanism alternative.

## ARK-009 — non-arithmetic transfer gate

Both seeds reached ordinary fact-set-disjoint held-out exact = **1.0**, but strict query-swap qualification failed (peak 0.383 / 0.360). No retention forks ran.

Important confound: the implemented diagnostic changes query **and reverses fact order**, so it does not isolate query conditioning.

**Status:** `TRANSFER_GATE_NOT_QUALIFIED`; LR-effect transfer remains `NOT_DEMONSTRATED`.

## ARK-010 — recovery after instability

Nine prospectively captured collapse states were forked on matched future tails. HIGH `1e-3` recovered sustained G90 in **8/9**; LOW `1e-5` in **2/9**. Mean RET90 `0.611` vs `0.111`; mean final OOD `0.949` vs `0.812`.

**Status:** low LR is protective while capability is present, but immediate low LR is usually poor for reacquisition after collapse. New hypothesis: **HIGH for acquisition/recovery -> LOW after confirmed capability for retention**. The switching policy itself is not yet tested.

## Program verdict

- Post-G90 LR protection: **replicated at Micro T2 scale**.
- Irreversible-forgetting interpretation of collapse: **rejected**; most HIGH continuations recover.
- Non-arithmetic transfer: **not demonstrated**.
- Adaptive state-dependent LR controller: **new hypothesis, not yet demonstrated**.
- V5-Core promotion: **not yet justified**.
