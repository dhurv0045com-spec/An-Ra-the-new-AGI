# Prompts and memory for work without another chief conversation

## Initial prompt — copy to the implementation agent

Continue BRAMASTRA from the latest pushed branch, preserving other agents' work. Read AGENTS.md, engineering/STATUS.md and engineering/ten_day_handoff_20260915/README.md. Follow TEN_DAY_EXECUTION.md as the execution schedule and engineering/FINAL_EXPERIMENT_EXECUTION.md plus engineering/final_delivery/REQUIREMENTS.json as the full technical/acceptance contract. Start editing production code; do not write another blueprint or stop at a one-hour subset. Complete every F01–F24 requirement, offline data artifact, source-bound build verifier, real E0–E6 consumers and runnable owner notebook. The latest agent PASS labels are provisional when their own gap fields describe missing integration. Use bounded Luna/Sol lanes only where useful with exclusive ownership. No local optimizer updates or accelerator launch; preserve the one owner two-T4/480-minute campaign. Commit useful progress, maintain a durable cursor, run focused checks and push scoped work. Continue across sessions until ready-to-launch evidence is complete or a precise external blocker prevents it. No further chief dispatch is needed for routine implementation.

## Resume prompt — use at the start of any later session

Resume FINAL-K8. Read engineering/ten_day_handoff_20260915/README.md and the latest progress cursor/report. Inspect Git status and commits. Verify which F requirements have actual accepted evidence; do not infer completion from file names or old PASS counts. Continue the earliest incomplete prerequisite and independent owned work. Preserve resource limits and existing artifacts. End with tested scoped code, updated cursor, exact remaining failures and a pushed commit; do not restart design or rerun the entire history.

## Review prompt — give to a separate inexpensive reviewer

Read-only review of the latest FINAL-K8 implementation. Read the final requirement inventory and submitted evidence. Trace actual compiler→trainer→cognitive consumer→environment→report paths, parent/optimizer identity, RSI ordering, deadline/update reductions and export restoration. Check claims against executable evidence. Prioritize missing consumer connections, impossible task information, hidden-label leakage, copied controls, fixture substitution and partial reports labeled PASS. Return only actionable findings with symbols, reproduction and required acceptance checks. Do not edit shared files or run optimizer steps; bound every diagnostic subprocess. A passing helper test is not full-path acceptance.

## Final release prompt

Perform fresh-checkout FINAL-K8 acceptance against the actual delivered source/data/runtime artifacts. Run the no-update build verifier and required integrated regression/rehearsal checks. Ensure F01–F24 have exact evidence, no remaining implementation TODO and source-bound report identities. Validate owner notebook commands, automatic E0 qualification, one immutable allocation, recovery/export and actual payload restores. Publish the full data artifact, compact manifests and final operator guide; push scoped source. Report ready_for_owner_experiment=true only if the owner can launch without another implementation patch. Actual CUDA qualification remains mandatory in E0; scientific claims await its subsequent registered results.

## Durable cursor format

Create a unique report directory per session; never overwrite evidence. Maintain `engineering/FINAL_K8_PROGRESS.md` as the small navigation cursor, not the evidence store. Use this format:

```text
Source revision and dirty owned paths:
Current schedule checkpoint and active requirement IDs:
Completed this session — production symbols and evidence links:
Executed commands — outcomes and measured time:
Still failing — exact assertion/consumer, not a generic estimate:
Next concrete edit:
Owned paths / other active owners:
Source/data/schema identities that changed:
Artifact paths and immutable report directory:
Local optimizer updates: 0
Owner accelerator run: not launched / actual owner-run identifier
Ready for owner experiment: false / evidence-backed true
```

The cursor can be updated as work proceeds. Keep failed-run receipts, earlier handoffs and completed experiment outputs immutable. If context is exhausted, the next agent reads this cursor and continues implementation instead of rereading every historical document.

## Owner-facing final message template

```text
Source commit:
Ready for owner launch: yes/no
Notebook and exact input artifact locations:
Local build/rehearsal checks and remaining GPU-only qualification:
Required owner steps:
Output/recovery location:
Remaining blocker, if any:
```

Do not ask the owner to design tasks, choose routine code changes or resolve already specified budget rules. Ask only when a genuinely new authorization/resource or unavailable required information is necessary. Do not promise AGI, a performance multiplier or completion on a calendar date without evidence.
