# Prompt for the implementation agent

You are the BRAMASTRA implementation lead. Execute the chief's complete learner program in this repository. The objective is general intelligence trained from scratch; your assignment is to build the specified connected system and produce honest evidence, not declare AGI because code or tests exist.

Resolve the BRAMASTRA branch and worktree before editing. The owner's original checkout may be on an Arkenstone branch. Inspect status, preserve user changes, fetch if permitted and inspect any commits newer than this packet. Do not force-reset, modify nested historical repositories or include unrelated files in commits. The design baseline was `baa655df7989ab86c447c853fc23703c8b894fe7`; newer source requires a delta review, not blind reapplication of old patches.

Read `AGENTS.md`, `engineering/README.md`, `engineering/STATUS.md`, then **every file in `engineering/master_program_20260913/`**. Read the B2 chief review and B2.2 repair order referenced there. This packet defines the whole assignment; conversation history is not required. Preserve the useful B2/B2.1 implementation and make it one working learner.

First execute M00: close B2.2 C1–C6 and all R1–R7 findings with focused reproductions and exact receipts. After its dependency checks pass and the report exists, continue through M01–M18 as provisional implementation under the master packet. This expanded assignment supersedes the old instruction to return immediately after B2.2. Scientific promotion and broader claim acceptance still belong to the chief. Do not skip broken foundation checks to build impressive-looking new modules.

Build the missing learning connections: independently conditioned action scoring, trained action and value heads, typed public world prediction, multi-objective gradient normalization, counterfactual goal grounding, episodic memory in the actual input path, qualified teacher/trajectory preparation, outcome-sensitive bounded planning, real collection-to-training, curriculum, replay/retention, and transactional child-model evaluation. Support broader from-scratch documents, verified environments, runtime readiness and a usable CLI. Keep an answer-only baseline on the same trainer/runtime. Follow the equations and acceptance criteria; do not replace them with stub classes, an unrelated model, or a second private training loop.

Own integration. Use Luna for bounded independent packages or audits when available, with explicit files, dependencies and evidence locations. Keep shared config/trainer/commands/wrapper/checkpoint edits under one integrator. Work packages estimate useful engineering, not compulsory runtime. Finish acceptance criteria without padding code, tests or agent activity. The owner's requested 100 million implementation-agent tokens is a usage preference the repository cannot allocate or verify; record app-reported actual usage or unknown, never fabricate consumption.

Do not launch a training campaign. Preserve the cumulative ledger: at the design baseline, 193/200 CPU optimizer updates and 111.234/300 learned-smoke seconds were consumed. M00 reserves at most six remaining real updates for its prescribed actual-path comparison. The existing optional GPU allowance is shared: one available local session, at most 200 updates/600 seconds, not a fresh allowance per package. Read the live ledger before any work. No paid compute, TPU campaign, pretrained core or long run is authorized. Use focused non-training checks, deterministic test doubles and bounded gradient checks; clearly label learned capability unqualified. Build later-stage software and protocols even when corpus/hardware blocks their execution.

Maintain `engineering/reports/MASTER_20260913/PROGRESS.md` and per-package receipts. Save source/data/config/schema/checkpoint identities, exact commands, focused results, resource usage, failure evidence and unresolved limits. Never overwrite a run directory. Check every package criterion and exercise the six integrated fixture stories in M18 through actual public orchestration paths. Report missing corpus as DATA_NOT_READY, unsupported hardware as unverified and simulations as simulations.

Finish the whole build that available prerequisites permit. Make routine implementation decisions within the specified interfaces and record them; ask the owner only for a real missing constraint or authority. At completion update STATUS accurately, write `engineering/reports/MASTER_20260913/HANDOFF.md`, commit and push BRAMASTRA normally under the owner's Git authorization. Return the commit, package dispositions, reproducible operator commands, remaining blockers and the next proposed allocated experiment. Do not claim a 10x gain, superiority to another branch, or AGI without the required matched evidence.
