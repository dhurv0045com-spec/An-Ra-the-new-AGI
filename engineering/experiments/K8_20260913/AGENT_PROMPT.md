# Agent prompt: build the K8 Kaggle experiment

You are the BRAMASTRA implementation lead. Build a runnable Kaggle notebook for the owner's newly authorized **single 7–8-hour session on two T4 GPUs**. The owner will execute it. Your task here is implementation, data preparation and notebook readiness; do not start a long session elsewhere.

Inspect the latest BRAMASTRA branch/worktree without overwriting user work. The design reviewed source `edee72726706e0f8f46ec98bb3c979ebc2930233`. Read root `experiment.md`, `engineering/experiments/K8_20260913/READINESS.md` and `campaign.json` completely, then the M00–M24 architecture/algorithm contracts they reference. K8 is the current milestone; finish its I01–I06 prerequisites efficiently rather than expanding unrelated modules.

The current branch contains useful cognition/RSI modules but is not ready for this GPU campaign: training/restore default to CPU; the objective router is not consumed by the canonical trainer; action/value/world helpers are inference-only; executive scoring is injected; and the generation runner is a fixture. Fix those connections through one shared trainer/model/runtime. A passing isolated unit suite does not establish learned execution.

Implement `python -m bramastra_lab.research.campaigns.k8` with prepare/validate/run/summarize/export, the single-writer supervisor, two isolated GPU workers, campaign reservations/deadlines and stateful resume. Deliver `notebooks/bramastra_k8.ipynb`, a hashed generated dataset bundle and the operator runbook. No pretrained weights or external LLM teacher are allowed. The owner can provide data, but the selected campaign uses qualified repository-generated worlds and tool tasks, so do not block on a huge internet corpus.

Execute the exact phase plan: E0 actual CUDA/gradient/resume gate; E1 answer-only versus unified supervision with two paired seeds; E2 cognitive/inquiry controls; E3 tool acquisition and retention; E4 gated shared-block architecture; E5 real model-origin method selection and self-selected successor update with P_fixed/P0 controls; E6 durable evidence/export. The entire campaign is at most480 elapsed minutes/960 provisioned GPU-minutes, including every attempt. Stop new training by minute450. Missing learned behavior must be blocked or labeled, never silently replaced by a scripted oracle or fixture.

Keep the old206-update CPU ledger immutable. The new allocation is only for the named owner-launched Kaggle campaign; local work uses non-learning checks and bounded gradient wiring. E0 learned checks/pilots are charged to K8. Do not rerun probes to capture output, reset clocks on restart or allocate eight hours to each phase. Use one integrator and Luna for bounded independent work with explicit ownership; no padded agents/tests/token consumption.

Pay particular attention to RSI attribution. Capture P0's actual generated method and pre-decision archive before applying it to P1; train P_fixed from the same P0 with the same data/time under M0. Capture all confirmation choices before any current-task outcomes exist. Score against matched measured method trials, preserve rejected/failing choices, and distinguish extra training from self-selected learning-method benefit. A valid method string or a successful fixture generation is not an RSI result.

Run focused local integration checks, build the source/data artifacts, validate notebook commands and write `engineering/reports/K8_BUILD/HANDOFF.md`. Report implemented versus locally tested versus CUDA-unverified status, precise remaining gates, source/data hashes and exact owner launch steps. Commit and push BRAMASTRA under existing authorization. Do not claim AGI or an unmeasured performance multiplier. The desired output is a reliable, informative eight-hour experiment that can tell us what to improve next.
