# Experiment specification — <ID / revision>

Status: draft / pilot / frozen / running / complete / interrupted / invalid.

## Scientific question

Hypothesis, alternative explanation, and outcome that would reject the hypothesis:

Primary contrast and estimand:

Controls, including diagnostic-only privileged conditions:

## Data and access

Generator/source identities, semantic split policy, train/probe/dev/strategy-validation/confirmation IDs:

Learner-visible inputs, teacher privileges, examiner-only data:

Deduplication, ambiguity and shortcut qualification:

## Model and learning

Initialization/parent identity, exact model configuration/parameter count, tokenizer prior:

Objective components, masks, normalization and terminal targets:

Optimizer, schedule, batch/update counts, rollout and replay distributions:

Inference policy, real-interaction allowance, planning/model-call allowance:

## Sampling and decision

Training seeds, independent semantic units, evaluation sample sizes:

Primary metric, practical effect, retention margins, intervals/tests:

Candidate count, multiplicity/sequential handling, pool retirement:

Stop/go, invalid-run and incomplete-run rules:

## Runtime and evidence

Measured hardware, remaining quota, compile/training/evaluation/storage budget:

Global deadline and checkpoint reserve:

Source/data/runtime hashes, artifact directory and remote durability requirements:

Pilot changes and freeze identity:

## Result (fill only after execution)

Raw outcomes, costs, failures, comparison, uncertainty, limitations and next decision:
