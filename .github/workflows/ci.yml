name: An-Ra CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install package and dependencies
        run: |
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -e ".[dev]"

      - name: Verify structure
        run: python scripts/verify_structure.py

      - name: Run unit and integration tests
        run: |
          python -m pytest tests/ \
            -x -q \
            --timeout=120 \
            -m "not gpu" \
            --ignore=tests/test_drive_session_manager_integration.py \
            --ignore=tests/test_v2_drive_artifacts.py
        env:
          PYTHONPATH: .

      - name: Run type checking on anra package
        run: mypy anra/ --strict --ignore-missing-imports

      - name: Run linting
        run: ruff check anra/ tests/

      - name: Check no hardcoded /content/ paths
        run: python -m pytest tests/test_no_hardcoded_root_paths.py -v

      - name: Innovation cycle dry run
        run: python scripts/run_innovation_cycle.py
        env:
          PYTHONPATH: .

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -e ".[dev]"

      - name: Verify key imports
        run: |
          python -c "import anra_brain; print('anra_brain OK')"
          python -c "import anra; print('anra package OK:', anra.__version__)"
          python -c "from anra.core.registry import MODEL_REGISTRY; print('registry OK:', MODEL_REGISTRY.list())"
          python -c "import identity.hal; print('identity.hal OK')"
          python -c "import memory.memory_router; print('memory_router OK')"
        env:
          PYTHONPATH: .

      - name: Lint with ruff
        run: ruff check anra/ tests/
        env:
          PYTHONPATH: .

      - name: Type check with mypy
        run: mypy anra/ --strict --ignore-missing-imports
        env:
          PYTHONPATH: .
