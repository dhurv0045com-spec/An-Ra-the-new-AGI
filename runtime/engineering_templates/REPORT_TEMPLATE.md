# Engineering deliverable: {title}

Generated by An-Ra `cad_generate` — **diagram scaffold, not certified design data**.

## Intent

{intent}

## Assumptions

- Geometry is **stylized** for visualization and iteration, not manufacturer drawings.
- Dimensions are placeholders unless you supplied verified numbers.
- OpenSCAD export requires `openscad` on PATH for `.stl` generation.

## Files

| File | Purpose |
| --- | --- |
| `{scad_path}` | Parametric OpenSCAD model |
| `{stl_path}` | Mesh export (if OpenSCAD ran) |

## Next verification steps

1. Replace placeholder diameters/lengths with sourced specs.
2. Cross-check mass flow / thrust claims with independent calculators (do not trust LLM prose).
3. Run symbolic checks on any numeric claims via `python scripts/anra.py --symbolic "..."`.
4. Store falsifiers in `identity/falsification_ledger.py` for design claims you adopt.

## Falsifier example

> This model would be wrong if fan diameter or core length contradict measured OEM cutaway data.
